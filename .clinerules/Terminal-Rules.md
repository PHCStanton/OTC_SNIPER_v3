# Always use Powershell command:
NOT '&&' but ';''=
User Terminal set to 'Default Terminal'

# PYTHON ENVIRONMENT AND DEPENDENCIES
The Python interpretor is set to QuFLX-v2 'çonda activate QuFLX'.
Dependencies are installed.
Double check before installing New Dependcies and check if you are in the conda environment QuFLX-v2.